diff -up esmf-8.4.0/src/addon/esmpy/pyproject.toml.orig esmf-8.4.0/src/addon/esmpy/pyproject.toml
--- esmf-8.4.0/src/addon/esmpy/pyproject.toml.orig	2023-02-02 15:49:31.164524520 +0000
+++ esmf-8.4.0/src/addon/esmpy/pyproject.toml	2023-02-02 15:54:40.991687183 +0000
@@ -1,17 +1,19 @@
 [build-system]
-requires = [ "setuptools>=41", "wheel", "setuptools-git-versioning"]
+#requires = [ "setuptools>=41", "wheel", "setuptools-git-versioning"]
+requires = [ "setuptools>=41", "wheel"]
 build-backend = "setuptools.build_meta"
 
 [project]
 name = "esmpy"
 description = "ESMF Python interface"
 # readme = "README.md"
-maintainers = [{author = "ESMF Core Team", email = "esmf_support@ucar.edu"}]
+#maintainers = [{author = "ESMF Core Team", email = "esmf_support@ucar.edu"}]
+maintainers = [{name = "ESMF Core Team", email = "esmf_support@ucar.edu"}]
 requires-python = ">=3.7"
 license = {text = "University of Illinois-NCSA"}
 dependencies = [
     "numpy",
-    "pytest-json-report",
+    #"pytest-json-report",
     'importlib-metadata; python_version < "3.8"',
     # setuptools-git-versioning shouldn't be needed here, but is
     # included as a workaround for problems with the build-time
@@ -19,27 +21,28 @@ dependencies = [
     # build-system section above). By including it here, we at least
     # ensure that this package will be available for a second or
     # subsequent pip install of esmpy.
-    'setuptools-git-versioning; python_version >= "3.10"',
+    #'setuptools-git-versioning; python_version >= "3.10"',
 ]
-dynamic = ["version"]
+#dynamic = ["version"]
 
-[tool.setuptools-git-versioning]
-enabled = true
-template = "{tag}"
-dev_template = "{tag}"
-dirty_template = "{tag}"
-starting_version = "8.4.0" # this is a backup for pip <= 22.0 where git-versioning doesn't work
+#[tool.setuptools-git-versioning]
+#enabled = true
+#template = "{tag}"
+#dev_template = "{tag}"
+#dirty_template = "{tag}"
+#starting_version = "8.4.0" # this is a backup for pip <= 22.0 where git-versioning doesn't work
 
-[tool.dynamic]
-version = "placeholder" # this is a placeholder for the version pulled with git-versioning
+#[tool.dynamic]
+#version = "placeholder" # this is a placeholder for the version pulled with git-versioning
+version="8.4.0"
 
 [tool.setuptools.packages.find]
 where = ["src"]
 exclude = ["doc*"]
 
-[tool.pytest.ini_options]
-testpaths = [
-    "src/esmpy/test/test_api",
-    "examples/test_examples.py", 
-    "src/esmpy/test/regrid_from_file/test_regrid_from_file.py", 
-]
+#[tool.pytest.ini_options]
+#testpaths = [
+#    "src/esmpy/test/test_api",
+#    "examples/test_examples.py", 
+#    "src/esmpy/test/regrid_from_file/test_regrid_from_file.py", 
+#]
